//! Build-script helper for the `litmask` crate.
//!
//! Intended to be invoked from a downstream crate's `build.rs` as a one-liner:
//!
//! ```ignore
//! fn main() {
//!     litmask_build::emit();
//! }
//! ```
//!
//! The `emit()` function is introduced in Task 5 per `docs/TASKS.md`.
