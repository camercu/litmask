//! `litmask-cli` — companion tool for `bind` and `inspect` operations.
//!
//! Subcommands land in Tasks 24–26 per `docs/TASKS.md`.

fn main() {
    eprintln!("litmask-cli: not yet implemented");
    std::process::exit(70); // EX_SOFTWARE
}
