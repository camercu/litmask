# litmask Developer-Experience Specification — Build-Sealed Keying (retired)

> **Status:** retired. This document has been folded into the canonical
> [`SPECIFICATION.md`](./SPECIFICATION.md). It is kept only as a pointer;
> its full history lives in git.

The build-sealed keying model it described is now implemented and specified
canonically:

- **Keying model & tiers** — `SPECIFICATION.md` §1.6 (providers), §1.7
  (build-sealed workflow), §2.4–§2.6 (build/runtime requirements).
- **Embedded-floor warning** — §1.3.2.
- **Profile-split diagnostics** — §1.9.5.
- **Design rationale, accepted residuals (I-R1–I-R7), and origin friction
  (F1–F7, S1)** — Appendix D.

There is no separate DevEx spec to maintain; edit `SPECIFICATION.md`.
