# Auto-generated by scripts/setup-dev.sh from .tool-versions.
# Edit .tool-versions instead and re-run `just setup`.
[toolchain]
channel = "1.88.0"
profile = "minimal"
components = ["clippy", "rustfmt", "llvm-tools"]
# `thumbv7m-none-eabi` is the bare-metal embedded target used by
# `just check-no-std` to cross-verify the runtime crate's
# `--no-default-features --features alloc` build (§2.10.1).
targets = [
  "thumbv7m-none-eabi",
  "x86_64-pc-windows-gnu",
  "aarch64-apple-darwin",
]
